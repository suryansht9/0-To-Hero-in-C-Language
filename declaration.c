#include <stdio.h>

int main() {
    int marks[5]; //this is the declaration of array
    printf("the size of this array in stack memory is :%d\n",sizeof(marks));
    //also we show the size of this array used in stack memory
    return 0;
}